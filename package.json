{
  "name": "pull-request-graph",
  "version": "0.0.1",
  "description": "Pull Request とブランチを取得し、Mermaid 形式で可視化する Node.js ツール。GitHub Device Flow を用いてプライベートリポジトリにも対応します。",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "open": "^10.1.0"
  }
}
